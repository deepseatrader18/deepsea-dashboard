SpeechRecognition==3.10.4
PyAudio==0.2.14
PyAutoGUI==0.9.54
# Unpinned (was ==1.26.4): that exact version has no prebuilt wheel for
# newer Python releases, forcing pip to compile it from source — which
# then fails without Visual Studio's C compiler installed. Let pip pick
# whichever numpy has a prebuilt wheel for the installed Python instead.
numpy
sounddevice==0.4.6
# Free, unlimited, no API key — Microsoft Edge's neural TTS. Replaced
# ElevenLabs because its free tier blocks all voices via the API.
edge-tts==6.1.19
playsound==1.2.2
python-dotenv==1.0.1
# Scrapes YouTube search results (no API key) so "gana sunao" can open an
# actual matching video instead of just youtube.com's homepage.
youtube-search==2.1.2
