# --- Binance API keys (create at binance.com -> Account -> API Management) ---
# IMPORTANT: create keys with "Enable Spot & Margin Trading" checked, but
# do NOT enable withdrawals on this key. Also restrict the key to this
# VPS's IP address under Binance's API key IP-access-restriction setting —
# without that, anyone who steals this .env file can trade (and IP
# restriction is your only defense besides that).
BINANCE_API_KEY=
BINANCE_API_SECRET=

# The master live/paper switch. Must be exactly "true" (lowercase) for
# real orders to be placed — anything else (unset, "false", a typo) runs
# the whole team pipeline and logs what it *would* have done instead.
# This is the fastest kill-switch: set to false and restart to stop all
# new trading instantly (existing open positions' OCO orders already sit
# on Binance's own servers and keep working even if this process is off).
LIVE_TRADING_ENABLED=false

# Which coin the team trades against (Binance quote asset), e.g. USDT.
QUOTE_ASSET=USDT

# --- Risk policy (conservative defaults — tune only once you understand
#     what each one does) ---
PER_TRADE_RISK_PCT=1
TAKE_PROFIT_PCT=0.6
STOP_LOSS_PCT=0.35
MAX_CONCURRENT_TRADES=2
DAILY_LOSS_CAP_PCT=3
SYMBOL_COOLDOWN_MIN=15

# --- Scanner tuning ---
SCAN_INTERVAL_MS=20000
VOLUME_SURGE_MULTIPLIER=3
MIN_QUOTE_VOLUME_24H=2000000
MIN_DELTA_QUOTE_VOLUME=50000

# --- Reporting to the Render dashboard (optional — trading still works
#     without this, you just won't see live status on the Crypto Trading
#     Room page) ---
# Same shared secret already used by the WhatsApp/laptop bridge — set the
# same value here as WHATSAPP_BRIDGE_TOKEN on the deepsea-dashboard Render
# service (or set a separate CRYPTO_BRIDGE_TOKEN on both sides if you'd
# rather keep it independent).
WHATSAPP_BRIDGE_TOKEN=
DASHBOARD_URL=https://deepsea-dashboard.onrender.com
